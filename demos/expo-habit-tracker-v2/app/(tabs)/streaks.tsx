/* /streaks — the Streaks tab: the meter-bridge of momentum. */

import TabScreen from "@/components/TabScreen";
import StreaksInstrument from "@/components/StreaksInstrument";

export default function StreaksTab() {
  return (
    <TabScreen>
      <StreaksInstrument />
    </TabScreen>
  );
}
